# Example HttpCanary that expects a 204 No Content response.
apiVersion: canary.iambarton.com/v1alpha1
kind: HttpCanary
metadata:
  name: sample-http-check-204
  namespace: default
spec:
  url: "https://httpbin.org/status/204"
  interval: 30
  expectedStatus: 204
