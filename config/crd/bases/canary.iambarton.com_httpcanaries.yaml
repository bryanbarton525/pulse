---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: httpcanaries.canary.iambarton.com
spec:
  group: canary.iambarton.com
  names:
    kind: HttpCanary
    listKind: HttpCanaryList
    plural: httpcanaries
    singular: httpcanary
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HttpCanary is the Schema for the httpcanaries API.
          It represents a single HTTP endpoint health check.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              HttpCanarySpec defines the desired state of HttpCanary.
              This is what the user fills in when they write their YAML.
            properties:
              expectedStatus:
                default: 200
                description: ExpectedStatus is the HTTP status code that indicates
                  success.
                maximum: 599
                minimum: 100
                type: integer
              interval:
                default: 30
                description: Interval is how often (in seconds) to run the check.
                minimum: 5
                type: integer
              url:
                description: URL is the HTTP endpoint to check.
                minLength: 1
                type: string
            required:
            - url
            type: object
          status:
            description: |-
              HttpCanaryStatus defines the observed state of HttpCanary.
              The controller fills this in — the user never sets it directly.
            properties:
              lastCheckTime:
                description: LastCheckTime is when the canary was last evaluated.
                format: date-time
                type: string
              lastStatus:
                description: LastStatus is the HTTP status code from the most recent
                  check.
                type: integer
              message:
                description: Message provides human-readable detail about the current
                  state.
                type: string
              phase:
                description: 'Phase is the current state: Healthy, Unhealthy, or Unknown.'
                enum:
                - Healthy
                - Unhealthy
                - Unknown
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
